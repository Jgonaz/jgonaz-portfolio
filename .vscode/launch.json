{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: npm run dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "nvm use 24 && npm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
